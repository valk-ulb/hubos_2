module.exports = class TabacAction {
    constructor(access, type, context ) { // default context value if not provided
        this.access = access;
        this.type = type;
        this.context = context;
    }

}