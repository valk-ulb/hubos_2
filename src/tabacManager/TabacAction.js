export default class TabacAction {
    constructor(access, type, context, period ) { // default context value if not provided
        this.access = access;
        this.type = type;
        this.context = context;
        this.period = period;
    }

}